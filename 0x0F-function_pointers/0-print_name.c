#include "main.h"

/**
 * print_name - prints a name when called.
 * @name: A string
 * @f: A function of a pointer
 */

void print_name(char *name, void (*f)(char *))
{

if (name != '\0' && f != '\0')
ptr(name);
}
