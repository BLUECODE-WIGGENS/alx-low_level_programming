#include "main.h"
#include <stdio.h>

/**
 * main -printing its name.
 *@argc: argument cuont
 * @argv: argument vector
 * 
 * return : 0.
 */

int main (int argc, char *argv[])
{
(void) argc;
printf ("%s\n", argv[0]);

return (0);
}
