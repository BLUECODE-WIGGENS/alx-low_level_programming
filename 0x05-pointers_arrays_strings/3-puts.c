#include "main.h"

/**
 * _puts - prints a string in stdout
 * @str: string to print
 *
 * Return: void
 */
void _puts(char *str)
{
        int i;

        for (i = 0; i[str] != '\0'; i++)
	{
	  _puts(&str);
		
	}
	_puts("\n");
}
